<div style="max-width:800px;margin:0 auto;padding:80px 40px;">
  <h1 class="section-title" data-reveal="up" style="margin-bottom:40px;">{{ page.title }}</h1>
  <div class="product-desc" data-reveal="up" data-reveal-delay="100">
    {{ page.content }}
  </div>
</div>
