{
  "name": "relier-builder",
  "version": "1.0.0",
  "description": "Website builder for tradesmen",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build-client": "cd client && npm run build",
    "install-all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "uuid": "^9.0.1"
  }
}
